# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

This is a graduate research project (Pós-Graduação em IA Aplicada, UniSENAI) analyzing long-term salinity (TDS) trends in wastewater at the Los Angeles–Glendale Water Reclamation Plant (LAGWRP). It is not a conventional software application — there is no build/lint/test tooling in the CI sense.

**Public deliverables on GitHub** are only: (1) the data-science pipeline (`scripts/script_00` through `scripts/script_30`, plus `script_00b`) with `data/` and `results/`; (2) the Streamlit dashboard in `huggingface_space/`; and (3) the didactic notebook under `notebooks/`. Remote: `github.com/igorfnogueira/projeto-aplicado`.

LaTeX article drafts, execution plans, decision logs (ADRs), process prompts, and related support files may exist on the local disk under gitignored paths (`Artigo/`, `prompts/`, `plano_projeto_TDS.md`, etc.). They are **not** part of the public repository. Agents working against the public tree must **not** treat filling or maintaining the article as a repository obligation.

## Repository layout

```
data/raw/           # eSMR CSVs, xlsx, PDSI raw, comparator plant CSVs
data/processed/     # canonical monthly datasets, flow series, PDSI series, LADWP extracts
scripts/            # script_00 … script_30 (+ script_00b)
utils/              # paths.py, experiment_tracking.py, validacao_utils.py
results/            # resultados_comparacao.*, *_resultados.*, comparison tables
results/figures/    # PNGs generated by the pipeline
notebooks/          # didactic notebook
huggingface_space/  # Streamlit app (own data/ + images/ copies)
```

All path constants go through `utils/paths.py` (`raw()`, `processed()`, `result()`, `figure()`). Run scripts from the repo root:

```bash
python scripts/script_00_preprocessamento.py
```

## Local-only support material

When present locally (gitignored / not on GitHub), files such as `Artigo/`, `plano_projeto_TDS.md`, `prompts/`, glossaries, and decision logs can inform pipeline work. Prefer public sources of truth for anything cited in commits or READMEs: `scripts/`, `results/`, `results/figures/`, and `notebooks/notebook.ipynb`. Never invent results, metrics, references, or conclusions.

## Data

- Raw data source: `data/raw/Los_Angeles_Reclamation_Plant_2026_additional_data2.xlsx`, an eSMR export. Split into `data/raw/TDS.csv`, `Chloride.csv`, `BOD.csv`, `Ammonia.csv`.
- CSV format: separator `;`, decimal `,` — read with `sep=';', decimal=','`.
- Key columns: `Location; Parameter; Analytical Method; Calculated Method; Qual; Result; Units; MDL; ML; RL; Sampling Date; ...; Latitude; Longitude; Receiving Water Body`.
- `Location` values `EFF-001` (pre-2012) and `EFF-001A` (after) are the **same physical point** — must be unified. `R-4`, `R-7`, `RSW-650`, `RSW-654` are receiving-water points (context only).
- Filter to `Units == 'mg/L'`. Prefer `Calculated Method == "Monthly Average (Mean)"` for the canonical monthly series.
- BOD non-detect treatment: three parallel canonical datasets in `data/processed/` (MDL/2, zero, ROS/Helsel) — do not silently pick one.
- Old notebook `projeto_aplicado_v1 (1).ipynb` — do not reuse; pipeline is built from scratch.

## Pipeline structure (implemented — `scripts/script_00` through `script_30`)

```
scripts/script_00_preprocessamento.py          # canonical monthly datasets from the 4 raw CSVs
scripts/script_00b_analise_censura_bod.py      # BOD non-detect treatment analysis (5 options, D-16)
scripts/script_01_mann_kendall_theilsen.py     # Mann-Kendall, Sen's slope, Theil-Sen, OLS
scripts/script_02_arima_sarima.py              # STL + ARIMA/SARIMA
scripts/script_03_random_forest_gridsearch.py  # Random Forest (CPU)
scripts/script_04_xgboost_lightgbm.py          # XGBoost/LightGBM (CUDA-capable)
scripts/script_05_prophet_bayesiano.py         # Prophet + Bayesian regression
scripts/script_06_correlacao_tds_amonia_bod.py # TDS↔Ammonia/BOD correlation
scripts/script_07_analise_estrutura_serie.py   # seasonality, ADF/KPSS, changepoints
scripts/script_08_baselines.py                 # naive, seasonal-naive, ETS, theta
scripts/script_09_svr_gp.py                    # SVR and Gaussian Process
scripts/script_10_detrend_arvore.py            # detrend + tree forecasting
scripts/script_11_multivariado_cloreto.py      # SARIMAX with chloride exog
scripts/script_12_hibrido_arima_prophet.py     # ARIMA+Prophet hybrid
scripts/script_13_deep_learning.py             # LSTM
scripts/script_14_diagnostico_residuos.py      # residual diagnostics
scripts/script_15_sintese_final.py             # consolidates methods / finalists
scripts/script_16_reconstrucao_vazao.py        # effluent flow from lb/day ÷ (mg/L × 8.34)
scripts/script_17_matriz_sensibilidade.py      # 9-item sensitivity matrix
scripts/script_18_pdsi_regimes.py              # NOAA PDSI vs. TDS cycles
scripts/script_19_wrtds.py                     # WRTDS flow-normalization
scripts/script_20_balanco_massa.py             # mass balance
scripts/script_21_cenarios.py                  # Monte Carlo climate scenarios
scripts/script_22_espaco_estados.py            # state-space / DLM
scripts/script_23_gam.py                       # GAM (P-splines)
scripts/script_24_regressao_quantilica.py      # quantile regression
scripts/script_25_intervencao_arimax.py        # SARIMAX intervention analysis
scripts/script_26_modelos_fundacionais.py      # Chronos-Bolt
scripts/script_27_cenario_climatico_caladapt.py # Cal-Adapt RCP 8.5 scenario
scripts/script_28_ladwp_tds_origem.py          # source-water TDS vs. effluent
scripts/script_29_bateria_pdsi_covariavel.py   # D-56: re-train with PDSI covariate
scripts/script_30_pdsi_finetuning_multilag.py  # D-57: PDSI multi-lag fine-tuning
```

Forecasting methods write to `results/resultados_comparacao.csv`/`.json` (append, never overwrite other methods) and figures to `results/figures/`. `script_28` writes `data/processed/ladwp_tds_origem_vs_efluente.csv`. `script_29`/`script_30` append `_com_pdsi` / `_pdsi_multilag` / `_pdsi_lagsel` rows.

GPU (RTX 4060 Ti 16GB, CUDA) is mainly useful for XGBoost/LightGBM; other methods are fine on CPU (~180 monthly points).

## Reference materials

Assignment/literature PDFs may exist locally (gitignored via `/*.pdf`). Nature Sustainability (2020) remains the professor-mandated anchor reference for interpreting rising TDS.
